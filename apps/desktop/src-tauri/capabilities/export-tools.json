{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "export-tools",
  "description": "Filesystem and SQL access for export module",
  "windows": ["main"],
  "permissions": [
    "sql:default",
    "sql:allow-execute",
    "fs:default",
    "dialog:default"
  ]
}
